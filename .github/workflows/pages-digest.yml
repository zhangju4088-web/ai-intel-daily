name: Publish Daily Digest to GitHub Pages

on:
  schedule:
    # Beijing 08:13 = UTC 00:13. Avoid the top-of-hour GitHub Actions rush.
    - cron: "13 0 * * *"
    # Backup triggers. The build step skips these if today's archive already exists.
    - cron: "43 0 * * *"
    - cron: "13 1 * * *"
    - cron: "13 2 * * *"
    - cron: "13 4 * * *"
  workflow_dispatch:
    inputs:
      digest_date:
        description: "Digest date to generate, for example 2026-06-17. Leave empty for today."
        required: false
        type: string
      extract_limit:
        description: "Number of top articles to read with body extraction and DeepSeek."
        required: false
        default: "16"
        type: string

permissions:
  contents: write
  pages: write
  id-token: write

concurrency:
  group: pages
  cancel-in-progress: false

jobs:
  build:
    runs-on: ubuntu-latest
    timeout-minutes: 30
    env:
      DEEPSEEK_API_KEY: ${{ secrets.DEEPSEEK_API_KEY }}
      DEEPSEEK_BASE_URL: https://api.deepseek.com
      DEEPSEEK_MODEL: deepseek-v4-pro
      DEEPSEEK_TIMEOUT_SECONDS: "18"
      EXTRACT_TIMEOUT_SECONDS: "8"
      FETCH_TIMEOUT_SECONDS: "12"
      TITLE_LOCALIZATION_CHUNK_SIZE: "4"
      FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: "true"
      INTELHUB_PROGRESS: "1"
      DIGEST_DATE: ${{ github.event.inputs.digest_date }}
      EXTRACT_LIMIT_INPUT: ${{ github.event.inputs.extract_limit }}
      SITE_AUTH_USERNAME: ${{ vars.SITE_AUTH_USERNAME }}
      SITE_AUTH_PASSWORD_SHA256: ${{ secrets.SITE_AUTH_PASSWORD_SHA256 }}
      SITE_AUTH_API_URL: ${{ vars.SITE_AUTH_API_URL }}
      SITE_REFRESH_WEBHOOK_URL: ${{ vars.SITE_REFRESH_WEBHOOK_URL }}
      SITE_REFRESH_ACTION_URL: https://github.com/zhangju4088-web/ai-intel-daily/actions/workflows/pages-digest.yml

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Build static digest
        run: |
          set -euo pipefail
          mkdir -p outputs public
          digest_date="${DIGEST_DATE:-$(TZ=Asia/Shanghai date +%F)}"
          date_args=()
          if [ -n "${digest_date}" ]; then
            date_args=(--date "${digest_date}")
          fi
          if [ "${GITHUB_EVENT_NAME}" = "schedule" ] && [ -f "public/archive/${digest_date}/daily-digest.json" ]; then
            echo "Archive for ${digest_date} already exists; skipping backup scheduled build."
            exit 0
          fi
          extract_limit="${EXTRACT_LIMIT_INPUT:-16}"
          echo "::group::Digest settings"
          echo "digest_date=${digest_date}"
          echo "extract_limit=${extract_limit}"
          echo "deepseek=$([ -n "${DEEPSEEK_API_KEY:-}" ] && echo enabled || echo disabled)"
          echo "fetch_timeout=${FETCH_TIMEOUT_SECONDS}s extract_timeout=${EXTRACT_TIMEOUT_SECONDS}s deepseek_timeout=${DEEPSEEK_TIMEOUT_SECONDS}s"
          echo "::endgroup::"
          if [ -n "${DEEPSEEK_API_KEY}" ]; then
            python -u -m intelhub.cli daily \
              "${date_args[@]}" \
              --extract-text \
              --extract-limit "${extract_limit}" \
              --summarize-with deepseek \
              --site-dir public \
              --out-json outputs/daily-digest.json \
              --out-html outputs/daily-digest.html
          else
            python -u -m intelhub.cli daily \
              "${date_args[@]}" \
              --summarize-with local \
              --site-dir public \
              --out-json outputs/daily-digest.json \
              --out-html outputs/daily-digest.html
          fi

      - name: Commit updated archive
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add public
          if git diff --cached --quiet; then
            echo "No public archive changes to commit"
          else
            git commit -m "chore: update daily digest archive"
            git push
          fi

      - name: Configure Pages
        uses: actions/configure-pages@v5

      - name: Upload artifact
        uses: actions/upload-pages-artifact@v3
        with:
          path: public

  deploy:
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    runs-on: ubuntu-latest
    needs: build
    steps:
      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4
